#include "model_data.h"

/**
 * Placeholder model data.
 *
 * Once you have trained and exported a TensorFlow Lite model from
 * ml/train_eye_state_model.py, you can convert the .tflite file to
 * a C array (for example, using the xxd utility) and replace the
 * contents of this file with the real model bytes.
 */

const unsigned char g_eye_state_model_data[] = {
    0x00
};

const int g_eye_state_model_data_len = sizeof(g_eye_state_model_data);

